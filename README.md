
# Prompt Template Repository

A structured repository for reusable AI prompt templates.

## Structure

- `templates/` → Core prompt templates
- `components/` → Reusable building blocks
- `examples/` → Sample usages
- `tests/` → Evaluation cases
- `tools/` → Helper scripts

## Example Usage

```bash
python tools/render_prompt.py

